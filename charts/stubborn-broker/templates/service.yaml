apiVersion: v1
kind: Service
metadata:
  name: {{ include "stubborn-broker.fullname" . }}
  labels:
    {{- include "stubborn-broker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stubborn-broker.selectorLabels" . | nindent 4 }}
