name: Publish Snapshots

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read

env:
  JAVA_VERSION: '25'
  JAVA_DISTRIBUTION: 'liberica'

jobs:
  publish:
    name: Publish Snapshots to Maven Central
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - name: Set up JDK
        uses: actions/setup-java@v5
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: ${{ env.JAVA_DISTRIBUTION }}
          cache: maven
          server-id: central
          server-username: CENTRAL_USERNAME
          server-password: CENTRAL_PASSWORD

      - name: Verify version is SNAPSHOT
        run: |
          VERSION=$(./mvnw help:evaluate -Dexpression=project.version -q -DforceStdout -pl build-parent)
          if [[ ! "$VERSION" == *-SNAPSHOT ]]; then
            echo "::error::Version $VERSION is not a SNAPSHOT — skipping snapshot publish"
            exit 1
          fi
          echo "Publishing snapshot: $VERSION"

      - name: Verify compilation
        run: ./mvnw compile -q

      - name: Build all modules locally
        run: ./mvnw install -DskipTests -DskipStubs=true -pl !e2e-tests

      - name: Deploy Java modules to Maven Central
        run: ./mvnw deploy -Ppublish -DskipTests -DskipStubs=true -pl build-parent,ui,broker,broker-api-client,broker-stub-downloader,broker-contract-publisher,broker-maven-plugin,stub-runner
        env:
          CENTRAL_USERNAME: ${{ secrets.CENTRAL_USERNAME }}
          CENTRAL_PASSWORD: ${{ secrets.CENTRAL_PASSWORD }}

      - name: Build Docker image
        continue-on-error: true
        run: ./mvnw spring-boot:build-image -pl broker -DskipTests -DskipStubs=true

      - name: Push Docker image
        continue-on-error: true
        run: |
          echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login -u "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin
          VERSION=$(./mvnw help:evaluate -Dexpression=project.version -q -DforceStdout -pl build-parent)
          docker push mgrzejszczak/stubborn:${VERSION}

