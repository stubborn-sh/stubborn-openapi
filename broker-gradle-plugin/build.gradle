plugins {
	alias(libs.plugins.plugin.publish)
	id 'java-gradle-plugin'
	id 'maven-publish'
}

group = 'sh.stubborn'
version = '0.1.0-SNAPSHOT'

java {
	toolchain {
		languageVersion = JavaLanguageVersion.of(25)
	}
}

repositories {
	mavenLocal()
	mavenCentral()
	maven { url 'https://repo.spring.io/milestone' }
}

dependencies {
	implementation libs.broker.contract.publisher
	implementation libs.spring.boot.starter.web
	implementation libs.jackson.databind
	testImplementation libs.junit.jupiter
	testRuntimeOnly libs.junit.platform.launcher
}

gradlePlugin {
	website = 'https://github.com/stubborn-sh/stubborn'
	vcsUrl = 'https://github.com/stubborn-sh/stubborn'
	plugins {
		broker {
			id = 'sh.stubborn.broker'
			implementationClass = 'sh.stubborn.oss.gradle.BrokerPlugin'
			displayName = 'Stubborn Broker Plugin'
			description = 'Publishes contracts to Stubborn Broker'
			tags.set(['spring-cloud-contract', 'contract-testing', 'broker'])
		}
	}
}

publishing {
	repositories {
		mavenLocal()
	}
}

tasks.named('test') {
	useJUnitPlatform()
}
